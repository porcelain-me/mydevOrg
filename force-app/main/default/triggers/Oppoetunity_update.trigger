trigger Oppoetunity_update on Opportunity (before insert, before update) {
    /*for (Opportunity o : Trigger.new) {
         if (trigger.isINSERT && o.Amount < 6000 )
             o.addError('please insert amount more than 6k');
          else if (trigger.isUPDATE && o.Amount <800000)
              o.addError('You are no good enough');
             
}*/
   // TriggerClass.trigger_method(trigger.new);
}